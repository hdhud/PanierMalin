{% extends 'base.html.twig' %}

{% block title %}Compose{% endblock %}

{% block body %}
    <h1>Compose</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compose.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ compose.quantite }}</td>
            </tr>
            <tr>
                <th>EstMarque</th>
                <td>{{ compose.estMarque ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compose_index') }}">back to list</a>

    <a href="{{ path('app_compose_edit', {'id': compose.id}) }}">edit</a>

    {{ include('compose/_delete_form.html.twig') }}
{% endblock %}
