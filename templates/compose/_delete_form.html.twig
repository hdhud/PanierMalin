<form method="post" action="{{ path('app_compose_delete', {'id': compose.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compose.id) }}">
    <button class="btn">Delete</button>
</form>
